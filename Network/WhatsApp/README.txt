Usage.
  4         We have the following command` SendMessage, AddGroup, JoinGroup, LeaveGroup, RemoveGroup, SendFile.
  5         All command aren't case sensitive (SendMessage == sendmessage).
  6         You should be leave the group before deleting them. Other cases we should have warning messages.
